set textwidth=80
set formatoptions+=t
set number
" Note - double quotes stands for comments in .vimrc

" This command is for cases when your backspace does not delete and arrow keys
" are mapped to A B C D
" set nocompatible 

" At times backspace does not work. This will fix it
" set backspace=2
